import { EmailPartPipe } from './email-part.pipe';

describe('EmailPartPipe', () => {
  it('create an instance', () => {
    const pipe = new EmailPartPipe();
    expect(pipe).toBeTruthy();
  });
});
